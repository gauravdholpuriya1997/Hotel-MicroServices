server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/gauravdholpuriya1997/Microservice-config-server  # connect to GITHUB CONFIG- SERVER
          clone-on-start: true